#! backend-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: backend
    namespace: job-tracker
    labels:
        com.docker.compose.project: job_tracker
        com.docker.compose.service: backend
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: job_tracker
            com.docker.compose.service: backend
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: job_tracker
                com.docker.compose.service: backend
                com.docker.compose.network.default: "true"
        spec:
            containers:
                - name: backend
                  image: job_tracker-backend
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: ConnectionStrings__DefaultConnection
                      value: "Server=sqlserver;Database=JobTracker;User Id=sa;Password=YourPassword123!;TrustServerCertificate=True;"
                  ports:
                    - name: backend-5000
                      containerPort: 5000
